# Default values for wunderwander-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: foldingbeauty/wunderwander-gitops
  tag: 0.1.4-dev
  pullPolicy: IfNotPresent
  
wunderwander:
  frontend:
  controller:
